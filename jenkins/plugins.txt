# Recommended Jenkins plugins for this lab (install via Plugin Manager or Configuration as Code plugins block)
# configuration-as-code
# git
# workflow-aggregator
# pipeline-stage-view
# junit
# credentials-binding
# docker-workflow
#
# The Jenkinsfile primarily uses shell + docker CLI available to the agent.
# Mounting docker.sock (see docker/lab-compose.yml) lets the controller build/push images in the lab.
