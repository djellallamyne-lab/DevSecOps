1. Deployed {{ .Release.Name }} into namespace {{ .Release.Namespace }}.

   Image: {{ .Values.image.repository }}:{{ .Values.image.tag }}

2. Port-forward:
   kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "demo-api.fullname" . }} 8080:8080

3. Smoke test:
   curl http://127.0.0.1:8080/api/hello
   curl http://127.0.0.1:8080/actuator/health

4. Security controls active in this chart:
   - non-root + readOnlyRootFilesystem
   - NetworkPolicy deny-all (+ DNS / monitoring / in-namespace)
   - dedicated ServiceAccount (token not auto-mounted)
   - Kyverno ClusterPolicies must also admit the Pod
